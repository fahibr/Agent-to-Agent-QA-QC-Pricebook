# Requires: Python 3.12.13
# Install (from project root):
#   py -3.12 -m pip install -r requirements.txt

# Core
python-dotenv==1.2.1
pandas==2.2.3
numpy>=1.26.4,<2.3.0

# Streamlit UI (A2A app)
streamlit==1.41.1

# Excel processing
openpyxl==3.1.5
et-xmlfile>=1.1.0,<2.0.0

# Azure OpenAI + LangChain agents
# langchain 1.x is required (create_agent API used by agent_1 / agent_2)
openai==2.45.0
langchain==1.3.12
langchain-core>=1.2.0,<2.0.0
langchain-openai==1.3.4
langchain-community==0.4.2
tiktoken==0.13.0

# Optional: vector store (not required for Agents A2A pipeline)
chromadb>=1.0.0,<1.1.0

# Authentication & Security
pycryptodome>=3.20.0,<4.0.0

# Windows COM automation (optional, Windows only)
pywin32>=308; sys_platform == "win32"
